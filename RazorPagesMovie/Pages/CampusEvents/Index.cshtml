@page
@model RazorPagesMovie.Pages.CampusEvents.IndexModel

@{
    ViewData["Title"] = "Campus Events";
}

<h1>Campus Events</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>

<form method="get">
    <p>
        Category:
        <select asp-for="EventCategory" asp-items="Model.Categories">
            <option value="">All</option>
        </select>

        Title:
        <input type="text" asp-for="SearchString" />

        <input type="submit" value="Filter" />
        <a asp-page="./Index">Back to full list</a>
    </p>
</form>

<table class="table">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.CampusEvent[0].Title)</th>
            <th>@Html.DisplayNameFor(model => model.CampusEvent[0].Description)</th>
            <th>@Html.DisplayNameFor(model => model.CampusEvent[0].EventDate)</th>
            <th>@Html.DisplayNameFor(model => model.CampusEvent[0].EventTime)</th>
            <th>@Html.DisplayNameFor(model => model.CampusEvent[0].Location)</th>
            <th>@Html.DisplayNameFor(model => model.CampusEvent[0].Category)</th>
            <th>@Html.DisplayNameFor(model => model.CampusEvent[0].SourceLink)</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CampusEvent) {
        <tr>
            <td>@Html.DisplayFor(modelItem => item.Title)</td>
            <td>@Html.DisplayFor(modelItem => item.Description)</td>
            <td>@Html.DisplayFor(modelItem => item.EventDate)</td>
            <td>@Html.DisplayFor(modelItem => item.EventTime)</td>
            <td>@Html.DisplayFor(modelItem => item.Location)</td>
            <td>@Html.DisplayFor(modelItem => item.Category)</td>
            <td>@Html.DisplayFor(modelItem => item.SourceLink)</td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.CampusEventId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CampusEventId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CampusEventId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>